You are auditing a resume-to-job analysis. Assume the analysis is WRONG.

You are given:
- The FULL resume text
- Requirements, each with a score and the evidence quoted for it

You are NOT given the original reasoning. Form your own judgment.

For each requirement, find the strongest argument that the score is wrong - IN EITHER DIRECTION.

TOO HIGH if:
- A keyword-only listing scores above 0.25 despite not demonstrating the capability
- Academic/hobby/coursework evidence scores above 0.25 when professional work is required
- A tool was inferred rather than stated

Do not invent constraints that are absent from the requirement. In particular, do not
assume professional employment is required unless the requirement says so. A resume
"Projects" heading does not by itself mean academic, hobby, or personal work. Judge the
actual evidence: deployment, monitoring, incident response, and explicit production
operation can fully support production-system experience regardless of section heading.
Likewise, do not demand advanced depth such as schema design, performance tuning, complex
queries, architecture, or leadership unless the requirement explicitly asks for it.
Explicit use of a technology in a professional role can fully support a plain
"experience with" requirement.

The score measures evidentiary support, not importance. A preferred requirement can be
fully supported and score 1.0. Its preferred importance is already handled separately by
weighting and routing, so never lower a score merely because the requirement is preferred.

Use the same evidence scale as the scorer:
- 1.0: clearly supported
- 0.75: mostly supported
- 0.5: partially supported
- 0.25: relevant but weak or indirect evidence
- 0.0: no relevant evidence at all

Do not lower 0.25 to 0.0 when a real keyword listing, academic project, or other
relevant adjacent evidence exists. Conversely, a score of 0.0 is too low when
such evidence exists, even if it does not satisfy the full requirement.

TOO LOW if:
- The FULL resume contains supporting evidence that was never quoted (retrieval missed it)
- The requirement was flagged low_retrieval_confidence

If you cannot find a strong argument, say so explicitly and leave the score unchanged.
Do not invent resume facts. Every RAISED score must quote verbatim resume text - your
quotes are checked against the source.

Full resume text:
{resume_text}

Requirements to audit:
{requirements_json}

Return valid JSON only with this exact structure:
{{
  "approved": false,
  "changed_requirements": [
    {{
      "requirement_id": "R3",
      "old_score": 1.0,
      "new_score": 0.25,
      "direction": "lowered",
      "reason": "Evidence names the term but does not demonstrate use.",
      "new_evidence": []
    }}
  ],
  "review_notes": ["Summary of what was changed and why."]
}}
